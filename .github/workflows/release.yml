name: Release

on:
  release:
    types:
      - published
  push:
    branches: [main]
    paths:
      - 'packages/extension-api/**'

env:
  NODE_VERSION: '22'

permissions:
  contents: write
  id-token: write
  packages: write

jobs:
  # Publish alpha version on push to main
  publish-alpha:
    name: Publish Extension API Alpha
    runs-on: ubuntu-latest
    if: "${{ github.event_name == 'push' && !contains(github.event.head_commit.message, 'chore(main): release') }}"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm for trusted publishing
        run: npm install -g npm@11.5.1

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build extension-api
        run: pnpm --filter @stina/extension-api build

      - name: Set alpha version and publish
        run: |
          cd packages/extension-api
          VERSION=$(node -p "require('./package.json').version")
          ALPHA="${VERSION}-alpha.$(git rev-parse --short HEAD)"
          echo "Publishing @stina/extension-api@${ALPHA}"
          npm version $ALPHA --no-git-tag-version
          npm publish --provenance --tag alpha --access public

  # Build and test before release
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          # Note: No cache to avoid native module conflicts

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        env:
          CI: 'true'

      - name: Rebuild better-sqlite3 for Node.js
        run: |
          echo "Node version: $(node --version)"
          # electron-rebuild is skipped in CI (see apps/electron/scripts/postinstall.js)
          npm rebuild better-sqlite3

      - name: Build packages
        run: pnpm build:packages

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test

  get-version:
    name: Get Version
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    outputs:
      tag: ${{ steps.get_version.outputs.tag }}
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - name: Get version from tag
        id: get_version
        run: |
          TAG_NAME="${{ github.event.release.tag_name }}"
          echo "tag=${TAG_NAME}" >> $GITHUB_OUTPUT
          echo "version=${TAG_NAME#v}" >> $GITHUB_OUTPUT

  # Build and publish extension-api to npm
  publish-extension-api:
    name: Publish Extension API
    needs:
      - build
      - get-version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm for trusted publishing
        run: npm install -g npm@11.5.1

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build extension-api
        run: pnpm --filter @stina/extension-api build

      - name: Upload JSON Schema as release asset
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: packages/extension-api/schema/extension-manifest.schema.json

      - name: Publish to npm
        run: |
          cd packages/extension-api
          npm publish --provenance --access public

  docker-api:
    name: Publish API Docker image
    needs:
      - build
      - get-version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/stina-api
          tags: |
            type=raw,value=${{ needs.get-version.outputs.version }}
            type=raw,value=latest,enable=${{ !github.event.release.prerelease }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  docker-web:
    name: Publish Web Docker image
    needs:
      - build
      - get-version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/stina-web
          tags: |
            type=raw,value=${{ needs.get-version.outputs.version }}
            type=raw,value=latest,enable=${{ !github.event.release.prerelease }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # Build Web app
  build-web:
    name: Build Web
    needs:
      - build
      - get-version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        env:
          CI: 'true'

      - name: Rebuild better-sqlite3 for Node.js
        run: npm rebuild better-sqlite3

      - name: Build packages
        run: pnpm build:packages

      - name: Build web
        run: pnpm --filter @stina/web build

      - name: Create archive
        run: |
          cd apps/web
          tar -czvf stina-web-${{ needs.get-version.outputs.version }}.tar.gz dist

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: apps/web/stina-web-${{ needs.get-version.outputs.version }}.tar.gz

  # Build API server
  build-api:
    name: Build API
    needs:
      - build
      - get-version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        env:
          CI: 'true'

      - name: Rebuild better-sqlite3 for Node.js
        run: npm rebuild better-sqlite3

      - name: Build packages
        run: pnpm build:packages

      - name: Build API
        run: pnpm --filter @stina/api build

      - name: Create archive
        run: |
          cd apps/api
          tar -czvf stina-api-${{ needs.get-version.outputs.version }}.tar.gz dist package.json

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: apps/api/stina-api-${{ needs.get-version.outputs.version }}.tar.gz

  # Build TUI/CLI
  build-tui:
    name: Build TUI
    needs:
      - build
      - get-version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        env:
          CI: 'true'

      - name: Rebuild better-sqlite3 for Node.js
        run: npm rebuild better-sqlite3

      - name: Build packages
        run: pnpm build:packages

      - name: Build TUI
        run: pnpm --filter @stina/tui build

      - name: Create archive
        run: |
          cd apps/tui
          tar -czvf stina-tui-${{ needs.get-version.outputs.version }}.tar.gz dist package.json

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: apps/tui/stina-tui-${{ needs.get-version.outputs.version }}.tar.gz

  # Build Electron for macOS
  build-electron-macos:
    name: Build Electron (macOS)
    needs:
      - build
      - get-version
    runs-on: macos-latest
    if: "!github.event.release.prerelease"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build:packages

      - name: Set Electron app version
        run: node -e "const p=require('./apps/electron/package.json');p.version='${{ needs.get-version.outputs.version }}';require('fs').writeFileSync('./apps/electron/package.json',JSON.stringify(p,null,4))"

      - name: Build Electron
        run: pnpm --filter @stina/electron build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          npm_execpath: pnpm.cmd

      - name: Upload macOS DMG
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: apps/electron/out/*.dmg
        continue-on-error: true

      - name: Upload macOS ZIP
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: apps/electron/out/*-mac.zip
        continue-on-error: true

      - name: Upload macOS auto-update manifest
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: |
            apps/electron/out/latest-mac.yml
            apps/electron/out/beta-mac.yml
            apps/electron/out/*.zip.blockmap
        continue-on-error: true

  # Build Electron for Windows
  build-electron-windows:
    name: Build Electron (Windows)
    needs:
      - build
      - get-version
    runs-on: windows-latest
    if: "!github.event.release.prerelease"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build:packages

      - name: Set Electron app version
        run: node -e "const p=require('./apps/electron/package.json');p.version='${{ needs.get-version.outputs.version }}';require('fs').writeFileSync('./apps/electron/package.json',JSON.stringify(p,null,4))"

      - name: Build Electron
        run: pnpm --filter @stina/electron build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows installer
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: apps/electron/out/*.exe
        continue-on-error: true

      - name: Upload Windows auto-update manifest
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: |
            apps/electron/out/latest.yml
            apps/electron/out/beta.yml
            apps/electron/out/*.exe.blockmap
        continue-on-error: true

  # Build Electron for Linux
  build-electron-linux:
    name: Build Electron (Linux)
    needs:
      - build
      - get-version
    runs-on: ubuntu-latest
    if: "!github.event.release.prerelease"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm build:packages

      - name: Set Electron app version
        run: node -e "const p=require('./apps/electron/package.json');p.version='${{ needs.get-version.outputs.version }}';require('fs').writeFileSync('./apps/electron/package.json',JSON.stringify(p,null,4))"

      - name: Build Electron
        run: pnpm --filter @stina/electron build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux AppImage
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: apps/electron/out/*.AppImage
        continue-on-error: true

      - name: Upload Linux auto-update manifest
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.get-version.outputs.tag }}
          files: |
            apps/electron/out/latest-linux.yml
            apps/electron/out/beta-linux.yml
        continue-on-error: true
