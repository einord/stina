# Implementation Status

> **What this is.** A living snapshot of how far the redesign-2026 implementation has gotten, written so a fresh contributor (or a fresh session of an AI assistant) can pick up cleanly without rereading the conversation that produced it. The spec sections (`01-vision.md` … `09-open-questions.md`) are the *what*; this file is the current *how* and *where*.
>
> **Maintenance.** Update when a phase lands, a convention emerges, or a gotcha bites. Stale notes are worse than none — if you see something that no longer matches reality, fix it in the same change as the code.

---

## Where we are in 30 seconds

- **Spec status**: §01–§08 are all in *Draft* and locked for v1. §09 is a living open-questions list. Three rounds of design-critic + spec-keeper passes landed; remaining open questions are either explicitly v2 or section-local.
- **Implementation phase**: foundation is in place + first user-visible UI slice is up. Specifically: schemas, repos, migrations, dev seeder, threads/messages API, inbox view with timeline (messages + inline activity entries).
- **What's wired but partial**: event-triggered threads — Phase 8a–8k landed. The full `emitEventInternal` runtime-only API is in place (§04 line 49 / acceptance line 204): `spawnTriggeredThread` in `@stina/orchestrator` is the single source of truth for the thread-create + appendMessage + decision-turn + failure-framing pipeline; both apps expose `emitEventInternal` accepting the full `ThreadTrigger` and `AppContent` unions (including `kind:'stina'` triggers). The untrusted-data wrapper in `provider.ts` now skips for `RUNTIME_EXTENSION_ID + system` content. Mail/calendar/work extensions still need rewrite to use the typed API; legacy `events.emit(name, payload)` coexists untouched. Phase 8g landed the v1 slice of closing-action normalization (§04 line 113–125): the orchestrator throws `ClosingActionMalformedError` when a `normal`-visibility reply has no text and no tool calls, routing it through the existing failure-framing path. Multi-action normalization rules (contradiction, multiple-normal, multiple-silent) wait for a richer producer output type. Phase 8h landed degraded mode (§04 lines 147–157): `DegradedModeTracker` in `@stina/orchestrator` tracks consecutive same-class failures and transitions the runtime into/out of degraded mode at the 5-failure/60s threshold; entry/exit are recorded as `extension_status` AppMessages on the anchor thread; `suppressNotification` is wired into `applyFailureFraming`'s return shape. Phase 8j landed notifications (§04 line 152–153): `NotificationDispatcher` pub/sub in `@stina/orchestrator`, `markNotified` monotonic guard in `ThreadRepository`, SSE stream + REST endpoints in `apps/api`, IPC bridge in `apps/electron`, `useNotificationStream` composable + `NotificationsBellButton` + `NotificationsDropdown` in `@stina/ui-vue`, wired into `MainNavigation` between Inkorgen and Aktivitetslogg. Phase 8k landed §06 severity-change cascade: `ToolSeveritySnapshotRepository` (read/write split, crash-safe at-least-once semantics), `applySeverityChangeCascade` in `@stina/orchestrator` (four-row transition table per §06 spec), `onToolSeverityObserved` callback wired into both apps. The §06 severity gate + policy management UI are in place (users create/revoke policies via the "Autonomi" sidebar). The per-tool redactor pipeline is wired host-side, but extension-registered tools still hit the no-redactor branch because redactor functions can't cross the worker IPC boundary in v1. Remaining §06 gaps: Escalate and Solve-differently collision options, extension-IPC redactor support, and Stina-suggested policies.
- **Branch**: all work is on `redesign-2026`. Multiple PRs are NOT being used; one long-lived branch with conventional commits is the working model.

---

## What's been built (chronological)

Each row links the commit hash to the kind of work. Read the commit messages for detail — they are intentionally verbose.

| Commit | Phase | What landed |
|--------|-------|-------------|
| `e868db4` … `44c9716` | Spec | Nine sections, three critic+keeper rounds, final audit. ~5500 lines of spec. |
| `7f12d04` | Phase 1 | Scaffolds for `@stina/threads` `@stina/memory` `@stina/autonomy` + shared types in `@stina/core`. SQL migrations land. |
| `83a77dd` | Phase 2a | `@stina/threads`: Drizzle schema, `ThreadRepository`, 17 tests. State-machine validation, monotonic surfacing, auto-revive on activity. |
| `75f72c1` | Phase 2b/2c/3 | `@stina/memory` + `@stina/autonomy`: schemas, repos with §07 hard-rule guards, recall provider registry, activity log. Migrations wired into apps/electron, apps/api, apps/tui. |
| `d017017` … `e93523a` | Phase A (testing) | `@stina/test-fixtures`: factories, three scenarios (`fresh-install`, `typical-morning`, `vacation-mode-active`), seed CLI. 25 tests. |
| `9fbad63` | Phase 4a | `apps/api`: `GET /threads`, `GET /threads/:id`, `GET /threads/:id/messages`, `POST /threads`, `POST /threads/:id/messages`. 19 route tests with stubbed auth. |
| `5d55628` | Phase 4b | `@stina/ui-vue`: `useThreads` composable, `InboxView` + four sub-components, "Inkorgen" navigation entry. Web-only (Electron IPC stubbed). |
| `90707a2` | Phase 4b polish | Activity log entries inline-rendered in thread timeline with severity-driven visual weight. New `GET /threads/:id/activity` endpoint. |
| `77e519c` | Phase 4c | Cross-thread activity log view: `GET /activity` with kind/severity/date filters, `ApiClient.activityLog`, Electron IPC mirror, `ActivityLogView` with chip filters and inspector pane, "Aktivitetslogg" entry in `MainNavigation`. |
| `a2ee75d` | Phase 4d polish | Recap-thread special rendering — amber accent, larger title, briefing-in-card per §05. |
| `b7315d9` | Phase 4d cross-platform | Electron IPC bridge so the desktop app consumes the same threads/messages/activity routes as web. |
| `57c6b82` | Phase 4d docs | `docs/architecture.md` rewritten around the new package layer + three-actor model. |
| `8ddda35` | Phase 5a | `@stina/orchestrator` package + canned-stub decision turn. POST /threads and POST /threads/:id/messages now run a synchronous decision turn that appends a `'stina'`-author reply. Producer is swappable via `threadRoutes` options for the future provider integration. |
| `5153cc8` | Phase 5b | §03 thread-start memory injection. `MemoryContextLoader` reads active standing instructions + profile facts matching `linked_entities` and folds them into `DecisionTurnContext.memory`. Default `DefaultMemoryContextLoader` is wired into `threadRoutes`; the canned stub reports the loaded count so memory is observable end-to-end. Loader is swappable via `threadRoutes` options. |
| `ee58e3a` | Phase 5c | `ProviderProducer` factory in `@stina/orchestrator` — drains a `ChatStreamDispatcher` (sync-wrap), assembles a system prompt from `DecisionTurnContext.memory`, maps the Stina message timeline into role-based `ChatMessage[]` (with §02 trust-boundary wrapper for app-author messages). |
| `2803f7d` | Phase 5d | apps/api wiring closes the loop end-to-end. `threadRoutes` now accepts a `getDecisionTurnProducer(userId)` factory; `server.ts` builds one that looks up the user's default model config (`UserSettingsRepository.getDefaultModelConfigId` → `ModelConfigRepository.get`), confirms the provider extension is currently registered, and dispatches via `ExtensionHost.chat()`. Falls back to the canned stub when extensions are off, no default config is set, or the configured provider isn't loaded — onboarding paths stay usable. |
| `e531784` | Phase 6a | SSE streaming server-side. `runDecisionTurn` accepts an `onStreamEvent` listener; both producers forward `content_delta` events through it, the orchestrator follows up with `message_appended` + `done` (or `error`). Two new endpoints — `POST /threads/stream` and `POST /threads/:id/messages/stream` — hijack the response and emit those events as `data: <json>\n\n` lines. The non-streaming endpoints are unchanged so existing UI keeps working until 6b lands. |
| `7092a6d` | Phase 6b | Streaming end-to-end through both transports + UI. `@stina/api-client` exposes `streamCreate` / `streamAppendMessage` (web: fetch + a small SSE parser; Electron: per-call requestId + `threads-stream-event` IPC channel; remote: inherits the web path). Electron IPC `threads-create` and `threads-append-message` now also run the decision turn — fixing a pre-existing gap where the Electron inbox never produced a Stina reply. `useThreads` switched both flows to streaming with a reactive `streamingDraft`; `InboxView.ThreadDetail` renders the in-flight reply as a placeholder card with a typing indicator (when no chunks arrived yet) or live text + blinking cursor (once chunks start flowing). |
| `c259b64` … `0c23a9c` | Phase 7a | Agentic tool loop in `createProviderProducer`. New `tools` + `executeTool` options activate the loop: each `tool_start` is collected, executed via the supplied function, and the result is appended as a `role: 'tool'` message before re-dispatching. Capped at `maxIterations` (default 10). New `tool_start` / `tool_end` events extend `TurnStreamEvent` and `ThreadStreamEvent`, propagating through SSE + IPC unchanged. apps/api + apps/electron wire the host's `getAllToolDefinitions()` and `executeToolCrossExtension()` (both promoted from `protected` to public). UI streaming card lists tool calls with a spinning ⚙ during run, ✓ on done, ✕ on error. **No severity gate yet** — high/critical tools execute the same as low/medium. |
| `d902e8b` | Phase 7b (rendering only) | Severity rendering on tool calls in the streaming card. `ToolDefinition` (interface + zod schema + emitted JSON schema) gains optional `severity?: ToolSeverity`; the type plumbs through `ExtensionHost.handleToolRegistered → ToolInfo → ExtensionToolAdapter → AdaptedTool → chat ToolRegistry's RegisteredTool / ResolvedToolDefinition → redesignProvider's tools[] → orchestrator producer`. Producer builds a `Map<id, severity>` (default `'medium'` for advertised-but-undeclared, `'high'` for not-in-map / hallucinated names), attaches `severity` to every `TurnStreamEvent.tool_start`. SSE + Electron IPC pass through unchanged. `ThreadStreamEvent.tool_start` and `StreamingToolCall` carry severity; `InboxView.ThreadDetail.vue` renders each tool row with §05 visual weight (low = quiet grey, medium = baseline, high = accent border-left + warm bg, critical = full rose border + heavier bg) reusing `InboxView.ActivityEntry.vue`'s colour tokens. **Visual weight only — critical-modal flow pending (item #7).** Persisted `StinaMessage.tool_calls` rendering NOT covered (no persistence yet — separate future step). |

| `1d65df0` | Phase 7c — ExtensionThreadHints | `AccentNameSchema` + `ExtensionThreadHintsSchema` wired end-to-end: extension-api schema + types → `ExtensionHost.getThreadHints()` → `GET /extensions/thread-hints` → `api-client.extensions.getThreadHints()` → `useExtensionThreadHints` composable → `InboxView.ThreadCard` accepts `extensionHints` prop (accent, icon, card_style, badge) → `InboxView.ThreadList` resolves hints per-thread (mail+calendar triggers only — scheduled triggers lack extension_id) → `InboxView.vue` loads on mount. Electron IPC bridge added (`extensions-get-thread-hints`). 5 ManifestValidator tests + 2 route tests + 12 ThreadCard logic tests added. CSS uses `--thread-accent` custom property so bordered + left-line both derive from the same colour token. |
| `b94eebc` | Dev tooling — `@stina/dev-test-extension` | New `packages/dev-test-extension/` inside the monorepo: declares `contributes.thread_hints` (plum bordered, 🧪 icon, DEV badge) in its manifest so the Phase 7c ExtensionThreadHints wiring is visually verifiable without the sibling-repo install dance. `scripts/install-dev-test-extension.mjs` copies the built dist into `data/extensions/local/dev-test/` and upserts `installed-extensions.json`. `pnpm dev:install-test-ext` added to root scripts (opt-in, not auto-hooked). `typicalMorning` seed updated: 2 of 3 extension-triggered threads now use `extension_id: 'dev-test'` (mail-from-customer + calendar-cancel) so the inbox shows hints applied vs. the `stina-ext-mail` news thread falling back to trigger-kind defaults. Zero new tests. |
| `1a0c4bf` | Phase 8 — tool call severity gate | `createProviderProducer` now gates every tool call by severity before executing: `low`/`medium` execute immediately; `high` requires an `AutoPolicy` (injected `lookupPolicy` callback, fail-safe blocks if absent); `critical` and hallucinated tool names block unconditionally. Three new injectable options on `ProviderProducerOptions`: `lookupPolicy`, `logAutoAction`, `logActionBlocked`. Blocked calls emit a `tool_blocked` `TurnStreamEvent` / `ThreadStreamEvent`, feed a synthesized `role:'tool'` error result back to the model (so the loop continues), and write `action_blocked` activity log entries with `chosen_alternative:'skip'` (v1 only). Policy-authorized high tools write `auto_action` entries with redacted I/O (`[redacted: redactor not yet wired]`). `apps/api/redesignProvider` wires the three callbacks via `AutoPolicyRepository` + `ActivityLogRepository`. UI streaming card gains a `blocked` status with 🚫 icon. 11 new tests in `severityGate.test.ts`. |
| `a2ffd04` | §08 migration runner — `@stina/migration` | New package `packages/migration/` with `ChatMigrator` (idle-gap heuristic, per-(user_id, conversation_id) iteration), `MigrationRunner` (BEGIN IMMEDIATE transaction, structured marker file, stale-marker recovery), `sanityChecks` (row-count parity, FK integrity, all-archived), and `schemaVersions` (schema_versions table management). 29 new tests: idle-gap heuristic exhaustive cases (empty DB, single-conversation no-gap, 3-day split, title rules, message type filtering, multi-user isolation, multiple conversations, malformed JSON), runner happy-path + sanity-check rollback + stale-marker recovery + marker JSON shape, and isolated sanity-check unit tests (FK-integrity and all-archived verified independently). |
| `db502b6` | §08 backup + app startup wiring | `BackupWriter` (VACUUM INTO with WAL note, per-spec filename `pre-redesign-2026-from-v<ver>-<ts>.stina-backup`), `MigrationInterruptedError` (crash-recovery structured error with `markerPath`), `runMigrationIfNeeded` (7-step guard sequence: marker check → schema_versions guard → legacy-table check → row count → backup → `MigrationRunner`). Wired into all three app entry points: `apps/api/src/server.ts` (`v0.5.0`), `apps/tui/src/index.ts` (`v0.5.0`), `apps/electron/src/main/index.ts` (`v0.36.0` via `app.getPath('userData')`). `@stina/migration` added to all three app `package.json` deps. 13 new tests (6 `BackupWriter`, 7 `runMigrationIfNeeded`). **v1 backup contract**: the backup is "pre-data-migration" — DDL tables exist but are empty; restoring gets the user to a state where the data migration has not yet run. Full gzip archive, quiescence, crash-recovery dialog, and `stina-restore` CLI are out of scope for v1. |
| `4958653` | §08 crash-recovery dialog | `readMigrationMarker` (new helper in `@stina/migration` — reads and parses the marker JSON, never throws, returns null on any error). Early marker check added BEFORE `initDatabase` in all three app entry points (API: `createServer`, TUI: `initializeApp`, Electron: `initializeApp`). API and TUI: structured multi-line `logger.error` with marker path, phase, timestamp, backup path, and three recovery options, then `process.exit(1)`. Electron: `showMessageBoxSync` three-option dialog — "Resume Migration" (unlinks marker + `app.relaunch()` + `app.exit(0)`), "Show Backup File" (`shell.showItemInFolder` + info dialog + `app.quit()`), "Quit and Contact Support" (`app.quit()`). Catch block refactored to early-return pattern: `MigrationInterruptedError` shows the dialog without the generic `showErrorBox`; all other errors still show `showErrorBox` and re-throw. Defense-in-depth catch handler retained for the (effectively impossible) race between the early check and `runMigrationIfNeeded`. 5 new tests in `readMigrationMarker.test.ts`. |
| `24a9acf` | Phase 2b — tool call persistence | `PersistedToolCall` interface added to `packages/core/src/messages/types.ts` and exported from `@stina/core`. `StinaMessage.content.tool_calls` now typed as `PersistedToolCall[]` (was `ToolCall[]` = unknown). `createProviderProducer` accumulates executed/blocked/errored calls in `tool_start` stream order via per-iteration ordered slots and merges into `allToolCalls` at turn end; severity carried on `pendingToolCalls` entries (not re-resolved from severityById). Return shape includes `tool_calls` when any tools ran. `InboxView.Message.vue` renders persisted tool calls (icon + name) above the text with §05 severity CSS mirroring `InboxView.ThreadDetail.vue`. 7 new tests in `providerProducer.test.ts`. **v2 follow-up**: full §05 three-piece blocked-row rendering (verb badge + "Approve now"), per-tool redaction symmetry, empty-text + tool-only turns, `tool_results` population. |
| `5a2560e` | ~~Phase B — Playwright E2E suite~~ | `POST /auth/e2e-session` route (gated by `STINA_E2E=true`) issues real JWTs for the seeded `e2e-test` user. `apps/web/playwright.config.ts` + `e2e/global-setup.ts` (deletes stale DB, seeds typical-morning fixture + e2e user + firstName via `UserSettingsRepository`) + `e2e/fixtures.ts` (`authenticatedPage` fixture with localStorage token injection) + `e2e/inbox.spec.ts` (3 tests: thread list renders, clicking opens detail, app-banner has `.im__app-banner` + `.im__untrusted`). `@playwright/test` added as devDependency to `apps/web`. `vitest.config.ts` gains explicit `exclude: ['apps/web/e2e/**']`. `apps/web` typecheck extended to cover `e2e/tsconfig.json`. |
| `4a2a43d` | §06 — Policy UI: clarity pass | First UX iteration of `PoliciesView` was hard to parse: row showed raw `tool_id` snake_case (read like a shell command), and the create form had a free-text "Instructions ID" field expecting the user to know a UUID. Pass: header rewritten with a plain-language explanation of what an autopolicy is. `PoliciesView.PolicyRow.vue` now shows the tool's human-readable name (looked up via `availableTools`) with the raw id as a small monospace secondary line; meta line collapses to a single readable row with separators ("Gäller alltid · Skapad för 2 minuter sedan"); `approval_count` is hidden when 0. `PoliciesView.CreateForm.vue` drops the free-text instruction-id field entirely (no list endpoint exists yet — deferred until standing-instruction list API lands). `PoliciesView.vue` introduces a "Aktiva policyer" section title, friendlier empty state, and passes `toolNameById` to rows. Also fixes `dev-test-extension/src/index.ts` — `ctx.tools` is optional, so it now throws if the permission is missing (typecheck fix). |
| `9596967` | §06 — Policy management UI | 4 HTTP API endpoints (`GET /policies`, `GET /policies/available-tools`, `POST /policies`, `DELETE /policies/:id`) added to `apps/api/src/routes/policies.ts` with shared `validatePolicyCreate` helper (validation order: tool exists → severity=high → instruction exists → no duplicate). `policies` namespace added to `ApiClient` interface and HTTP client. Electron IPC bridge: 4 handlers in `apps/electron/src/main/ipc.ts` + preload bridge entries + renderer IPC client namespace. `usePolicies` Vue composable. `PoliciesView.vue` + `PoliciesView.PolicyRow.vue` + `PoliciesView.CreateForm.vue` (`resetForm` exposed so parent only resets on success). "Autonomi" `MainNavigation` entry (`shield-01` icon). `currentView.ts` type + `AppShell.vue` view switch updated. `resetToolRegistryForTesting` exported from `@stina/chat`. 11 route tests in `policies.test.ts` (happy-paths, ordering, and all error cases). `memory_change` entry on revoke inherits tool severity (fallback 'low'). `06-autonomy.md` updated to reflect sidebar placement. |
| `e18e417` | §06 — Per-tool redactor wiring | `ToolRedactor` interface added to `@stina/extension-api` (`types.contributions.ts`, re-exported from `types.ts` and `index.ts`). `ToolDefinition.redactor?: ToolRedactor` field on the runtime interface (not in the zod schema — functions can't cross IPC; doc comment explains the intentional asymmetry). `RegisteredTool` and `ResolvedToolDefinition` in `@stina/chat/ToolRegistry` gain `redactor?`; `getToolDefinitions()` forwards it. `createProviderProducer` builds a `redactorById: Map<string, ToolRedactor>` alongside `severityById`; the `logAutoAction` callsite now applies the redactor when present (`flagged_for_review: false`) or falls back to `[redacted: no redactor declared]` + `flagged_for_review: true` (spec §06 "flagged for review" branch). `logAutoAction` callback type updated: `tool_input`/`tool_output` widen from literal string to `Record<string, unknown> | string`; `flagged_for_review: boolean` added. `redesignProvider.ts` forwards `redactor: t.redactor` in the tools mapping and `flagged_for_review` into the activity log `details`. Stale `'redactor not yet wired'` sentinel removed from all code and comments. `severityGate.test.ts` updated: existing test renamed to no-redactor branch (asserts sentinel + `flagged_for_review: true`); one new test for the redactor-present branch (asserts redacted objects + `flagged_for_review: false`). **Remaining gaps**: v1 closes the redactor wiring gap on the host side; ALL extension-registered tools (mail/calendar/work) currently fall into the no-redactor branch because redactor functions cannot cross IPC. Closing the extension-IPC gap is a separate tracked step. |

| `150d43c` | Phase 8a — emitEvent: typed event API + dev-test driver | `EmitEventInput`/`EmitEventResult`/`ExtensionThreadTrigger`/`ExtensionAppContent` added to `@stina/extension-api`. `EventsAPI.emitEvent` method available to all extensions (reuses `events.emit` permission). `EventsHandler` extended with `events.emitEvent` IPC path: deny-list rejects `user`/`stina` trigger kinds, enforces trigger↔content kind pairing, stamps `source.extension_id` + `trigger.extension_id` from host identity. `EmitThreadEventCallback` type wired through `ExtensionHostOptions` → `NodeExtensionHost` → `createNodeExtensionRuntime` → both `apps/api` and `apps/electron`. Callback creates thread, appends typed `AppMessage`, runs decision turn (fail-safe: errors logged, thread visible without Stina reply). `dev_test_emit_test_mail` tool (medium severity) added to dev-test extension alongside existing high-severity tool. 9 EventsHandler unit tests + 2 `emitEvent.test.ts` integration tests. |

| `188f162` | Phase 8b — failure mode framing | `applyFailureFraming` helper added to `packages/orchestrator/src/applyFailureFraming.ts` and exported from `@stina/orchestrator`. `RUNTIME_EXTENSION_ID = 'stina-runtime'` constant added to `packages/core/src/messages/types.ts` and exported from `@stina/core`. Both `apps/api/src/server.ts` and `apps/electron/src/main/index.ts` replace the bare `logger.warn` catch body with `applyFailureFraming({ threadRepo, activityLogRepo, logger }, { thread_id, error })`. The helper appends a `system`-kind `AppMessage` (Swedish framing: "Jag kunde inte bearbeta..."), calls `markSurfaced`, and writes an `event_handled` activity-log entry with `details.failure: true` — each guarded by its own try/catch so partial failures don't cascade. Scope note: malformed-output framing (requiring `runDecisionTurn` to return a structured `{ ok: false, reason }`) is explicitly deferred. 2 new integration tests in `emitEvent.test.ts` (producer-throws path + framing-append-throws path). **Update (Phase 8g):** malformed-output framing landed via throw-propagation rather than the structured-return refactor — `ClosingActionMalformedError` thrown by the validator flows through the same `applyFailureFraming` path as any other producer error. The structured-return approach turned out unnecessary. |
| `dac3239` | Phase 8f — `emitEventInternal` runtime-only thread spawning API | `spawnTriggeredThread` helper added to `packages/orchestrator/src/spawnTriggeredThread.ts` and exported from `@stina/orchestrator`. Accepts the full `ThreadTrigger` + `AppContent` unions (including `kind:'stina'` triggers and `kind:'system'`/`kind:'extension_status'` content). `EmitThreadEventInput` in `@stina/extension-host` widened to use `ThreadTrigger`/`AppContent` directly from `@stina/core`. **Pure derivation helpers `deriveTitleFromAppContent` + `deriveLinkedEntities` (plus internal `parseMailFrom`, `truncate200`) moved from `@stina/extension-host` to `@stina/threads/triggerDerivation.ts`** so `@stina/orchestrator` can import them without inverting the package layer (correct direction is `extension-host → threads`, never `orchestrator → extension-host`). `extension-host` re-exports both for back-compat with existing app importers and gains `@stina/threads` as a direct dep. `deriveTitleFromAppContent` extended to cover `system` (returns `content.message` truncated to 200 codepoints) and `extension_status` (returns `"${extension_id}: ${status}"` truncated); `deriveLinkedEntities` extended to return `[]` for both new kinds, signature simplified to take a plain `{trigger, content}` object instead of the full `EmitThreadEventInput`. Both `apps/api/src/server.ts` and `apps/electron/src/main/index.ts` refactored: `emitThreadEvent` callback now delegates to `spawnTriggeredThread` (pure refactor), and each app exposes a typed `emitEventInternal(input)` function stamping `RUNTIME_EXTENSION_ID` by default. Untrusted-data wrapper fix in `provider.ts`: runtime-origin `system` content (`extension_id === RUNTIME_EXTENSION_ID && kind === 'system'`) rendered without the `[OPÅLITLIG EXTERN DATA …]` header; all other content keeps the wrapper. 23 new tests across 5 new/extended files: `spawnTriggeredThread` unit tests (happy path, failure path with TWO system messages, title override, surfaced after turn, derived title); `deriveTitleFromAppContent` cases for `system` + `extension_status`; `deriveLinkedEntities` cases for `system` + `extension_status` → `[]`; `providerProducer` wrapper-exception tests (runtime-origin system skips wrapper; extension-origin system keeps it); `emitEventInternal` integration tests asserting (a) thread created, (b) AppMessage with `source.extension_id === 'stina-runtime'`, (c) `first_turn_completed_at` set, (d) thread visible in default `GET /threads`, (e) no failure activity entry on success path; type-level `@ts-expect-error` test in `@stina/extension-api` confirming the public `EmitEventInput` rejects `kind:'stina'` and `kind:'user'`. Test suite now 588. |
| `b6ebfd3` | Phase 8e step B — `MemoryContextLoader` consumes `RecallProviderRegistry` | Closes the consumer side of §03 acceptance line 152 + the parallel-query contract on line 98. `MemoryContext` extended with required `recall_results: RecallResult[]` field; `DefaultMemoryContextLoader` constructor gains optional 3rd `recallProviderRegistry` and 4th `logger` args (back-compat for two-arg callers preserved). On thread-start, fact lookups and per-entity recall queries (`{ query: entity.ref_id, scope: ['extensions'], limit: 5 }`) run **in parallel** via a single `Promise.all`. Per-entity contract is best-effort string match — providers responsible for ignoring unrecognized ref_ids; v1 has no entity-type discriminator on `RecallQuery`. Results dedupe via tuple-keyed `Map<string, Map<string, RecallResult>>` on `(source_detail, ref_id)`, sort by score desc, capped to `MAX_RECALL_RESULTS_PER_TURN = 15`. Token-budget guards: `truncateResultContent` (per-result content > 500 chars → 499 + `…`); `applyRecallSectionBudget` (section-total cap `MAX_RECALL_SECTION_CHARS = 8000`). Registry's `Promise.allSettled` semantics keep one bad provider from blocking the load; `onError` callback wired so silent failures surface in logs. `formatMemorySection` in `provider.ts` appends `## Information från extensions` after `linked_facts` (section ordering preserved). Canned stub note now mentions `Z extensionsnotering(ar)` so the full pipeline is observable end-to-end without a real model. Wired at all four `apps/` callsites: `apps/api/src/server.ts` (emitThreadEvent), `apps/api/src/routes/threads.ts` (user-message path; pino → `Logger` adapter so `onError` logs aren't lost), `apps/electron/src/main/index.ts`, `apps/electron/src/main/ipc.ts`. Integration test `apps/api/src/__tests__/recall.threadStart.test.ts` exercises the real worker round-trip via dev-test's echo provider and asserts the persisted Stina message contains `"1 extensionsnotering"`. 19 new tests (11 loader + 6 pure-helper + 2 rendering); existing tests updated to add `recall_results: []` to `MemoryContext` literals. **Production consumer is now wired**; `stina-ext-people` registering a real provider (R3) will close the user-visible loop. |
| `e4a147a` | Phase 8g — closing-action normalization (v1 slice) | `ClosingActionMalformedError` class + `normalizeClosingAction` validator added to `packages/orchestrator/src/closingActionNormalization.ts`. `runDecisionTurn` invokes the validator between the producer call and `appendMessage` — a malformed output throws and propagates through the existing try/catch, flowing into `applyFailureFraming` on the event path. Both new symbols exported from `@stina/orchestrator`. 8 unit tests in `closingActionNormalization.test.ts`; 3 integration tests added to `runDecisionTurn.test.ts`; 1 end-to-end integration test added to `apps/api/src/__tests__/emitEvent.test.ts` asserting system framing message + `error_class: 'ClosingActionMalformedError'` in the activity log + gate lifted. **Deferred normalization cases** (require richer producer output type): multi-action contradiction (one normal + one event_silenced), multiple-normal retention, multiple-silent retention. These presuppose the producer can emit multiple actions per turn — that protocol doesn't exist yet. **User-initiated path** intentionally unchanged: the throw flows through the swallow-and-log catch in `apps/api/src/routes/threads.ts` and the user sees no reply (existing pattern). Symmetric failure framing on the user path is a deliberate non-goal for v1. |
| `3223b33` + follow-ups | Phase 8e step A — recall provider extension API surface | `RecallProviderRegistry` is now reachable from extensions. New `recall.register` capability permission (`packages/extension-api/src/types.permissions.ts`); `RecallAPI` on `Context` (`ctx.recall.registerProvider(handler)`) returning a `Disposable`; mirrored `RecallQuery` / `RecallResult` / `RecallScope` types in extension-api (no `@stina/core` import — same pattern as `ExtensionThreadTrigger`). Worker→host: `recall.registerProvider` / `recall.unregisterProvider` via `RequestMethod`. Host→worker: dedicated `recall-query-request` / `recall-query-response` message pair (NOT on `RequestMethod` — that union is worker→host only) keyed by `requestId` on a `PendingRequestManager`. Disposable-generation rule: re-registering increments a generation; `dispose()` only sends unregister if its generation still matches the current slot, so stale disposables are silent no-ops. Auto-unregister on `unloadExtension` defensively. `recallPending.rejectAll()` on worker stop in both `NodeExtensionHost` and `WebExtensionHost` so in-flight queries don't leak until the 30s timeout. Single shared `RecallProviderRegistry` instantiated in `apps/api/setup.ts` and `apps/electron/main/index.ts`; injected into the host. `dev-test-extension` updated: declares `recall.register` and registers a trivial echo provider for end-to-end testing. **Spec acceptance §03 line 152 is now met for the registration side.** **Production consumer of the registry remains zero in this step**: `MemoryContextLoader` does not yet call `query()` — that's step B. 26 new tests (7 host-handler, 3 worker-side disposable-guard, 2 round-trip integration via real worker thread, plus existing registry tests still passing). |
| `da0dec9` | Phase 8d — linked-entity derivation from AppContent | `deriveLinkedEntities(input: EmitThreadEventInput): EntityRef[]` added to `packages/extension-host/src/ExtensionHost.handlers.events.ts` (next to `deriveTitleFromAppContent`) and exported from `@stina/extension-host`. Pure function. `mail` content → one `person` `EntityRef` keyed by lowercased parsed email (named-format `"Name <email>"` → display = trimmed name; bare email → display = lowercased email; malformed = no `@` → display = trimmed raw, ref_id = lowercased trimmed raw); `snapshot.excerpt = "${from}: ${subject}\n${snippet}"` truncated to ≤200 codepoints. `calendar` → one `calendar_event` ref (ref_id = `event_id`, display = `title`, excerpt = `${title} · ${ISO_starts_at}${location ? ' · ' + location : ''}`). `scheduled` → `[]` (job_id is a scheduler-internal handle, not a domain entity). No separate `mail` ref — trigger already pins it; YAGNI. `extension_id` on each ref = `trigger.extension_id` (source-ownership rule, §04 line 60). Wired into `apps/api/src/server.ts` and `apps/electron/src/main/index.ts` `emitThreadEvent` callbacks. v2 resolution-via-installed-extensions runs as a separate async pass after this helper; signature stays sync. 14 new tests (13 unit in `deriveLinkedEntities.test.ts` + integration assertion extension in `emitEvent.test.ts`). |
| `418e037` | Phase 8c — pending-first-turn invisibility gate | `first_turn_completed_at INTEGER` column added to `threads` table via `packages/threads/src/db/migrations/0002_add_first_turn_completed_at.sql` (wrapped in `BEGIN;…COMMIT;` for atomicity; backfills existing rows with `last_activity_at`; partial index `idx_threads_first_turn_done`). `Thread` interface in `@stina/core` gains `first_turn_completed_at: number \| null`. Drizzle schema, `rowToThread`, and `repo.create()` updated. New `markFirstTurnCompleted(id, at?)` repository method — monotonic, idempotent, mirrors `markSurfaced`. `list()` default: filters `first_turn_completed_at IS NOT NULL`; new `includePending?: boolean` opt-in. `runDecisionTurn` lifts the gate unconditionally after `appendMessage` succeeds (in its own try/catch). `applyFailureFraming` reworked to four-step pattern: framing append → markSurfaced → markFirstTurnCompleted → activity log; steps 2+3 gate on `framingOk`. Test fixtures: `makeThread` defaults `first_turn_completed_at` to `created_at`; new `makePendingThread` factory; seed.ts updated. 10 new tests added (incl. positive+negative compose-with-surfacing assertions). |

| `32e1a1e` | Phase 8h — degraded mode (§04 lines 147–157) | `DegradedModeTracker` class added to `packages/orchestrator/src/degradedMode.ts` with `recordFailure` + `recordSuccess` + `isInDegraded` API. `DEGRADED_MODE_FAILURE_THRESHOLD = 5` and `DEGRADED_MODE_WINDOW_MS = 60_000` exported as tunable constants. State machine rules: consecutive same-class failures outside degraded mode (different class resets counter — known limitation per spec); all failures aggregate once degraded; anchor = threshold-tripping thread (5th), NOT the first; window inclusive at exactly 60 000 ms. `applyFailureFraming` gains optional `tracker?: DegradedModeTracker`; on `enteredDegraded: true` writes a second `extension_status: degraded_mode_entered` AppMessage on the anchor thread + transition `event_handled` activity entry; return type widened from `Promise<void>` to `Promise<{ suppressNotification: boolean }>` (forward-compatible — callers discard the value). `spawnTriggeredThread` gains optional `tracker?: DegradedModeTracker`; calls `recordSuccess` inside the try block immediately after `runDecisionTurn` succeeds; on `exitedDegraded: true` writes `extension_status: degraded_mode_exited` AppMessage on the anchor thread + transition `event_handled` entry (both best-effort: errors swallowed, tracker state still resets). `extension_status.status` enum in `packages/core/src/messages/types.ts` gains `'degraded_mode_entered' \| 'degraded_mode_exited'`; no exhaustive-switch breakage (all existing usages interpolate `content.status` as a string or switch on `content.kind`). Both `apps/api/src/server.ts` and `apps/electron/src/main/index.ts` instantiate one `DegradedModeTracker` per host and pass it to both seams. Both exported from `@stina/orchestrator`. 20 new tests: 13 in `degradedMode.test.ts` (threshold/window/cross-class/cross-class-while-degraded/exit/reset/isolation/aggregation), 3 in `applyFailureFraming.test.ts` (entry message, 6th no-extra-message, swallowed-anchor-error), 4 in `spawnTriggeredThread.test.ts` (exit message + log entry, swallowed-exit-error); 2 new E2E tests in `emitEvent.test.ts` (5-failure entry, 5+1 exit). **v1 limitations**: in-memory state resets on restart; no actual notifications fire; hardcoded thresholds; user-initiated turns excluded from tally. |

| `25059c8` | Phase 8i — scheduled-jobs migration to typed events (§04 lines 168–176) | `SchedulerJobRequest.emit` field added to both `@stina/scheduler` and `@stina/extension-api` (dual-mirror pattern) with cross-referenced doc comments. `SchedulerFireEvent.emit` optional field plumbed through from `fireJob` reading `row.emitJson`. New SQL migration `packages/scheduler/src/migrations/0004_add_emit_config.sql` adds `emit_json TEXT` column (NULL-safe, no backfill). Drizzle schema gains `emitJson: text('emit_json')`. `assertValidJob` validates `emit.description` (non-empty string) and `emit.payload` (plain object if set). `SchedulerService.schedule` JSON-stringifies `emit` into `emit_json` on insert and upsert. `fireJob` reads `emit_json` via `safeParseEmit` — on parse failure logs a warn and falls through to legacy path (fail-safe). Both `apps/api/src/server.ts` and `apps/electron/src/main/index.ts` `onFire` handlers branch: when `event.emit` is set, logs an info-level audit signal and fire-and-forgets `emitEventInternal` (void + .catch — only shape that fits the synchronous callback signature); source intentionally omitted so `emitEventInternal` defaults to `RUNTIME_EXTENSION_ID` (runtime is emitter; extension is configurator; scheduler row's `extensionId` column preserves "which extension scheduled this" audit trail); otherwise legacy `notifySchedulerFire` path runs unchanged. `docs/guides/adding-an-event-source-extension.md` gains a "Shorthand — let the host emit for you" subsection (sibling of the existing canonical pattern section). 9 new tests: 8 in `SchedulerService.test.ts` (schedule-with-emit persists emit_json; schedule-without-emit is NULL; empty description throws; non-object payload throws; fireJob with emit produces emit field; fireJob without emit produces undefined; corrupted emit_json falls through with warn; migration 0004 apply test) + 1 integration test in `apps/api/src/__tests__/scheduledEmit.test.ts` (emit-shorthand job spawns scheduled thread with source=RUNTIME_EXTENSION_ID; notifySchedulerFire not called). **apps/electron wiring is structurally identical to apps/api**; api test covers the SchedulerService → emitEventInternal contract. Per §04 line 172 the scheduler considers the job successful as soon as the event is enqueued; Stina-side failures are tracked in the spawned thread's activity log. Legacy `appendInstruction` jobs continue working untouched (deprecation window per spec line 174). |

| `474703e` | Phase 8j — notifications (§04 lines 152–153) | `NotificationDispatcher` class added to `packages/orchestrator/src/notificationDispatcher.ts` with `subscribe` (returns disposable) + `dispatch` (synchronous, swallows listener errors) API. `NotificationEvent` interface carries `thread_id`, `user_id`, `title`, `preview` (≤140 codepoints), `kind: 'normal'\|'failure'`, `trigger_kind?`, `extension_id?`, `notified_at`. `markNotified(id)` in `ThreadRepository` widened from `Promise<void>` to `Promise<boolean>` — returns `true` on first write, `false` if already notified (monotonic guard). `ListThreadsOptions` gains `notifiedOnly?: boolean`; `list()` filters `notifiedAt IS NOT NULL` and orders by `notifiedAt DESC` in that mode. `runDecisionTurn` gains `notificationDispatcher?`, `notifyOnSurface?` (default false), `notifyUserId?` options; dispatches only when `notifyOnSurface: true` AND `markNotified` returns `true`. `makePreview` helper truncates text to 140 codepoints. `applyFailureFraming` gains `notificationDispatcher?` + `notifyUserId?`; dispatches failure notification (preview = `FAILURE_FRAMING_TEXT`) after all degraded-mode bookkeeping, gated on `framingOk && !suppressNotification`. `FAILURE_FRAMING_TEXT` extracted as a module-level constant (single source of truth for both `appendMessage` content and notification preview). `spawnTriggeredThread` gains `notificationDispatcher?` + `notifyUserId?`; passes them to both callees with `notifyOnSurface: true`. Both `apps/api/src/server.ts` and `apps/electron/src/main/index.ts` instantiate one `NotificationDispatcher` per host and wire it. `GET /notifications/stream` (SSE, hijacked, per-user bridge via `event.user_id !== userId` filter, keepalive comment every 25 s) and `GET /notifications` (REST, `notifiedOnly: true` list) added to `apps/api/src/routes/notifications.ts`. Electron IPC bridge: `notificationDispatcher.subscribe` forwards events on `notifications-stream-event` channel; `notifications-list` IPC handler wraps the repo list. Preload `notificationsStreamSubscribe` + `notificationsList`. `NotificationEvent` interface + `notifications` namespace (`streamSubscribe` + `list`) added to `@stina/api-client` types and HTTP client (`EventSource` with token query param + `withCredentials`). IPC client mirrors both methods. `tag?: string` added to `NotificationOptions` in `@stina/shared` for browser Notification API deduplication. `useNotificationStream` composable seeds from `api.notifications.list` on mount, subscribes to live stream, prepends to `recent`, increments `unreadCount`, fires OS-level `maybeShowNotification` when `document.hidden` (with `tag: notif:${thread_id}`). `NotificationsBellButton.vue` wraps a `NavigationButton` with an unread badge (outside the slot, absolute-positioned over the button) and toggles `NotificationsDropdown`. `NotificationsDropdown.vue` renders the `recent` list with bell/alert-triangle icons, title, preview, Swedish relative timestamp (`relativeTime`). Both wired into `MainNavigation.vue` between Inkorgen and Aktivitetslogg; `select-thread` event switches view to inbox and bubbles the thread ID. 14 new tests: 4 in `notificationDispatcher.test.ts`, 5 in `runDecisionTurn.notify.test.ts`, 2 new in `applyFailureFraming.test.ts`, 3 in `notifications.stream.test.ts`. **v1 limitations**: `unreadCount` resets on tab close (in-memory); events during brief disconnect are lost (no buffering); threads not user-scoped in DB (v1 single-user). |

| `17cad33` | Phase 8k — §06 severity-change cascade | `MemoryChangeCascadedFrom` union type (`{ kind: 'severity_change' \| 'log_entry' \| 'extension_uninstall' ... }`) added to `packages/core/src/autonomy/types.ts` and exported from `@stina/core`. `StinaTriggerReason` gains `'system_notice'` in `packages/core/src/threads/types.ts`. New SQL migration `packages/autonomy/src/db/migrations/0002_add_tool_severity_snapshots.sql` adds `tool_severity_snapshots` table (composite PK `extension_id + tool_id`). Drizzle schema extended with `toolSeveritySnapshots` table. `ToolSeveritySnapshotRepository` (read/write split: `compare` + `recordSeen`) exported from `@stina/autonomy/db`. `AutoPolicyRepository.revokeAllForTool(toolId)` added (returns deleted rows). `applySeverityChangeCascade` added to `packages/orchestrator/src/severityChangeCascade.ts`: implements §06 four-row transition table — lowers (any → low/medium, critical → high) are silent no-ops; `low\|medium → high` spawns a `system_notice` awareness thread; `→ critical` revokes all tool policies in a synchronous `db.transaction(...)` + writes `memory_change` audit entries with typed `cascaded_from` details + spawns a `system_notice` thread (revocation-focused vs. awareness-only Swedish text based on N). `ExtensionHostOptions.onToolSeverityObserved` optional callback added; `handleToolRegistered` invokes it fire-and-forget (errors swallowed + logged). `NodeExtensionRuntimeOptions.onToolSeverityObserved` added and threaded through `adapters-node`. Both `apps/api/src/server.ts` and `apps/electron/src/main/index.ts` instantiate `ToolSeveritySnapshotRepository` and wire the `onToolSeverityObserved` callback with `compare → (no-change: recordSeen) / (changed: applySeverityChangeCascade → recordSeen)`. 13 new tests: 4 in `toolSeveritySnapshotRepository.test.ts`, 6 in `severityChangeCascade.test.ts`, 1 integration test in `apps/api/src/__tests__/severityChange.integration.test.ts`, 2 in `autonomyRepositories.test.ts` (`revokeAllForTool`). **Remaining §06 gaps**: Escalate/Solve-differently collision options, extension-IPC redactor support, Stina-suggested policies, combined severity-change-AND-uninstall (spec line 154). |

| `2544231` | Phase 8l — welcome thread on first boot | New SQL migration `packages/autonomy/src/db/migrations/0003_add_runtime_markers.sql` adds generic `runtime_markers` table (composite PK `marker_key + user_id`). Drizzle schema gains `runtimeMarkers` table. `RuntimeMarkersRepository` (two methods: `has` + `set` idempotent upsert) exported from `@stina/autonomy/db`. `spawnWelcomeThreadIfNew` helper added to `packages/orchestrator/src/welcomeThread.ts` (exported from `@stina/orchestrator`): checks marker `'welcome_thread_v1'` keyed by userId, spawns a `kind:'stina'` + `reason:'system_notice'` thread with `kind:'system'` content + explicit title `'Välkommen till Stina'`, writes marker after successful spawn. `WELCOME_MESSAGE_TEXT` module constant (Swedish 5-paragraph welcome, single source of truth). `WelcomeThreadDeps` interface declares `emitEventInternal` inline to avoid inverting the package layer. Marker write order: spawn first, set after success (at-least-once: duplicate on crash is harmless). Both `apps/api/src/server.ts` and `apps/electron/src/main/index.ts` import `RuntimeMarkersRepository` + `spawnWelcomeThreadIfNew` and fire-and-forget the call after `setupExtensions` completes; user resolved via `defaultUserId` preference falling back to sole-user in DB (mirrors `emitEventInternal`'s own resolution path). 8 new tests: 3 in `runtimeMarkersRepository.test.ts` (has-false for unset key, set-then-has, userId isolation), 4 in `welcomeThread.test.ts` (first call spawns + sets marker, second call is no-op, emitEventInternal throws → no marker, marker write fails after spawn → returns spawned:true), 1 integration test in `apps/api/src/__tests__/welcomeThread.integration.test.ts` (synthetic emitEventInternal + real repos + NotificationDispatcher: asserts trigger/title/content/marker correct; notification dispatcher received one event; second call is no-op). |

| `22cd36d` | §05 blocked-row "Approve now" affordance | `tool_blocked` stream event gains `tool_id: string` + `chosen_alternative: 'skip'` fields (mirrored in `@stina/api-client`'s `ThreadStreamEvent`); `provider.ts` emit updated to populate both. `StreamingToolCall` in `@stina/ui-vue` gains optional `input`, `tool_id`, `blockedReason`, `chosenAlternative` fields; `applyStreamEvent` captures them from `tool_start` and `tool_blocked`. `InboxView.ThreadDetail.vue` streaming card: blocked rows now render a three-piece layout (verb badge, tool-call + abbreviated input, blocker reason) plus a "Godkänn nu" button for `no_matching_policy` rows; approval state tracked per row in `Map<id, state>` ref; post-approval label "✓ Policy skapad — gäller nästa gång" communicates that the current turn is not retried. `InboxView.ActivityEntry.vue`: `action_blocked` entries render the same three-piece layout using `details.tool_id`, `details.reason`, `details.chosen_alternative`, `details.tool_input`; same "Godkänn nu" button; duplicate-409 errors shown as "✓ Policy redan skapad". 6 new unit tests in `packages/ui-vue/src/composables/__tests__/useThreads.streamingTools.test.ts`. |

**Test count**: 672 tests pass (666 prior + 6 new — `StreamingToolCall` state transitions). **Typecheck**: clean across all packages and apps.

---

## Architecture in code

The package layer rules from `AGENTS.md` plus the new redesign-2026 packages form this stack (top depends on bottom):

```
apps/{web,electron,api,tui}
        │
        ▼
@stina/chat  ← still owns the legacy single-thread world
        │
        ▼  (chat ALSO depends on the new packages going forward)
@stina/orchestrator  (depends on threads + memory + autonomy)
        │
        ▼
@stina/autonomy  (depends on threads + memory)
        │
        ▼
@stina/memory    (depends on threads)
        │
        ▼
@stina/threads
        │
        ▼
@stina/core      ← types only, no runtime
```

The redesign-2026 packages **cannot import from `@stina/chat`** — that would invert the layer.

The new packages each follow the same skeleton:

```
packages/<name>/
├── package.json           workspace deps + Drizzle + nanoid
├── tsconfig.json          extends ../../tsconfig.base.json
├── tsup.config.ts         ESM+CJS, splitting:false, copies migrations to dist
└── src/
    ├── index.ts           barrel
    ├── db/
    │   ├── index.ts       schema + repo exports + getXxxMigrationsPath()
    │   ├── schema.ts      Drizzle table definitions matching the SQL
    │   ├── repository(ies).ts
    │   └── migrations/
    │       └── NNNN_*.sql
    └── __tests__/
        └── *.test.ts      vitest, in-memory SQLite, applies the package's own migrations
```

Tests apply the package's *own SQL migrations* against an in-memory DB — that way schema drift between SQL and Drizzle would surface immediately.

---

## Conventions that aren't in the spec but matter

These emerged during implementation and are now load-bearing:

### `tsup.config.ts` MUST set `splitting: false` for any package that ships migrations

If chunk splitting is on, tsup hoists `getXxxMigrationsPath()` into a shared chunk at `dist/chunk-*.js`, breaking the `import.meta.url`-based path resolution because the chunk lives at `dist/`, not `dist/db/`. Symptom: app boots, migrations runner silently skips paths that "don't exist", new tables never appear. Mirrors how `@stina/chat` keeps its function inlined.

### No separate DTO layer for redesign-2026 types

The `@stina/core` types (`Thread`, `Message`, `ThreadTrigger`, `AppContent`, …) are *already* the over-the-wire shape: snake_case fields, unix-ms timestamps as numbers, JSON-serializable. The frontend already imports from `@stina/core`. Don't add an `*-dto.ts` parallel hierarchy — extra busywork with two types to keep in sync. If we later need different serialization (e.g. ISO strings), add mappers, not new types.

### Use `resetDatabaseForTests()` in route tests

`closeDb()` from `@stina/adapters-node` resets the connection-layer singleton, but the `appDatabase.ts` layer has its *own* cached `database` variable that survives. Without resetting it, the second test's `initDatabase` returns a Drizzle wrapper around a closed handle — symptom: `"The database connection is not open"`. Pair `closeDb()` and `resetDatabaseForTests()` in `beforeEach`/`afterEach` of any test that swaps DB files.

### Auth stubbing in route tests

Routes use `requireAuth` from `@stina/auth`. Stubbing the real auth service is heavy — the `requireAuth` preHandler only inspects `request.isAuthenticated` and `request.user`, so the standard test pattern is:

```ts
app.decorateRequest('isAuthenticated', false)
app.decorateRequest('user', null)
app.addHook('onRequest', async (request) => {
  ;(request as any).isAuthenticated = true
  ;(request as any).user = { id: 'test-user', role: 'user' }
})
```

See `apps/api/src/routes/__tests__/threads.test.ts` for the canonical example.

### Foreign keys in test SQL

If your migration's `CREATE TABLE` references another package's table (`memory` references `threads(id)`), an in-memory test that only loads *your* package's migrations errors with `"no such table"` at create time — even if FK enforcement is off. Two patterns work:

- **Stub the referenced table**: `sqlite.exec('CREATE TABLE threads (id TEXT PRIMARY KEY)')` before applying memory migrations. See `packages/memory/src/__tests__/memoryRepositories.test.ts`.
- **Apply all packages' migrations together** (the integration pattern). See `packages/test-fixtures/src/__tests__/seed.test.ts`.

### `better-sqlite3` rebuild gotcha

`pnpm install` runs Electron's `postinstall` which rebuilds `better-sqlite3` against Electron's Node ABI. After a fresh install your tests will fail with `NODE_MODULE_VERSION` mismatch. Fix:

```sh
pushd node_modules/.pnpm/better-sqlite3@*/node_modules/better-sqlite3 \
  && npx --yes prebuild-install && popd
```

Worth automating in `dev-tools/` if it keeps happening.

### Drizzle better-sqlite3 transactions are SYNC

`db.transaction(callback)` accepts a sync callback only — returning a Promise throws `"Transaction function cannot return a promise"`. Inside the callback use `.run()`, `.all()`, `.get()` synchronously, even on `.values()` / `.set()`. See `ThreadRepository.appendMessage` for the working pattern.

### Commit subject must not be PascalCase

Commitlint config rejects subjects like `feat: InboxView landed` because of the capital I. Use lowercase: `feat: inbox view landed`. Body copy is unrestricted.

### `first_turn_completed_at` is the structural invisibility gate

Any code path that *creates* a thread but does not synchronously run a decision turn must leave `first_turn_completed_at` null until the turn finishes. The thread will not appear in `GET /threads` (default list) until `markFirstTurnCompleted` fires. Both `runDecisionTurn` (success path) and `applyFailureFraming` (failure path, only after the framing append succeeds) call this. New decision-turn callsites must do the same.

`surfacing=background` filter still excludes pending threads. The `IS NOT NULL first_turn_completed_at` filter composes with the `surfacing` filter — there is no surface where pending threads are visible by default.

### Runtime-emitted AppMessages use `source.extension_id: 'stina-runtime'` (constant `RUNTIME_EXTENSION_ID`)

When the host runtime appends an `AppMessage` directly (e.g. failure framing, future degraded-mode aggregate notification, future post-upgrade welcome thread), it stamps `source.extension_id` as the runtime-reserved value `'stina-runtime'`, exported from `@stina/core` as `RUNTIME_EXTENSION_ID`. Don't reuse an extension id; that breaks audit semantics and conflates runtime-origin messages with extension-origin messages.

### `pnpm dev:seed` exit codes

`0` = help / success, `2` = usage error (unknown scenario), `1` = runtime error. Don't let pnpm flag help-mode as a script failure — that creates noise around CLI usage (`ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL`).

---

## Driving the next step (`/redesign-step` skill)

For any meaningful chunk of new work, invoke the project skill at `.claude/skills/redesign-step.md`. It scripts a four-role team — project lead (you), critic (challenges the brief before code), implementer (writes the code), reviewer (compares output against the spec) — with explicit quality gates that fresh sessions otherwise tend to skip. Use it for any step that adds a schema, route, UI surface, or orchestrator wiring. Skip it only for pure cosmetics.

Trigger phrases the skill recognizes: `/redesign-step`, "kör nästa redesign-steg", "fortsätt på redesign-2026", "next redesign step". You can pass an explicit task ("ExtensionThreadHints wiring") or leave it open and let the skill propose from this doc's "Natural next steps".

## Dev workflow

### Visual exploration with seeded data (no AI required)

```sh
# One-time setup after pulling: install the dev-test extension (verifies §05 thread hints)
pnpm dev:install-test-ext        # copies dist + upserts installed-extensions.json

DB_PATH=/tmp/stina-demo.db pnpm dev:seed typical-morning --fresh
DB_PATH=/tmp/stina-demo.db STINA_MASTER_SECRET=dev pnpm dev:web
# open http://localhost:3002, log in, click "Inkorgen"
# → two cards with plum bordered border + 🧪 icon + DEV badge (dev-test hints)
# → one card with sky left-line + ✉ icon (fallback — no matching extension installed)
```

Three scenarios are registered: `fresh-install`, `typical-morning`, `vacation-mode-active`. Add new ones in `packages/test-fixtures/src/scenarios/`.

### Tests

```sh
pnpm test                    # full suite, currently 620 tests
pnpm test packages/threads   # path filter
pnpm typecheck               # global typecheck
```

### When migrations land

Each package owns numbered SQL migrations under `src/db/migrations/`. The runner in `@stina/adapters-node` picks them up via `getXxxMigrationsPath()` exports. Apps must register the new package's path in their `initDatabase({ migrations: […] })` call — three apps to update: `apps/api`, `apps/electron`, `apps/tui`.

---

## Natural next steps

Rough effort labels: **S** = single sitting, **M** = multi-sitting, **L** = multi-day.

### Polish track (visual / UX)

1. ~~**Recap-thread special rendering**~~ — landed in `a2ee75d`; the recap-trigger thread now renders with the §05 amber accent + larger title + briefing-in-card.
2. ~~**Severity rendering on tool calls**~~ — landed (Phase 7b above) for the in-flight streaming card only: `tool_start` events carry `severity` end-to-end and the streaming card renders rows with §05 visual weight. **Critical-modal flow still owed** (caught by item #7's severity-gate work). **Persisted-message tool-call rendering still owed** — see new sibling step below; depends on tool-call persistence landing first.
2b. ~~**Tool call severity rendering on persisted `StinaMessage.tool_calls`**~~ — landed (Phase 2b). `PersistedToolCall` type persisted to `StinaMessage.content.tool_calls` by the provider producer; `InboxView.Message.vue` renders them with §05 severity CSS. ~~**v2 follow-up: full §05 three-piece blocked-row rendering (verb badge + "Approve now")**~~ — landed: streaming card and persisted `action_blocked` entries both render intent + blocker reason + verb badge + "Godkänn nu" button for `no_matching_policy` rows. **Remaining v2 follow-ups**: per-tool redaction symmetry with activity log, empty-text + tool-only turns.
3. ~~**`ExtensionThreadHints` wiring**~~ — landed (Phase 7c). `AccentName` + `ExtensionThreadHints` added to extension-api schema and types; `ExtensionHost.getThreadHints()` resolves hints per-extension; `GET /extensions/thread-hints` route; api-client `getThreadHints()`; `useExtensionThreadHints` composable; `InboxView.ThreadCard` consumes accent/icon/card_style/badge via `extensionHints` prop (falls back to trigger-kind defaults). Lookup limited to mail+calendar triggers (only those carry `extension_id`). **Install dialog preview still owed** (§06). **`snippet_field` rendering still owed** (depends on AppContent kind registry).
4. ~~**Activity log under ☰**~~ — landed as `ActivityLogView`; v1 ships with kind / severity / dream-pass / auto-action filter chips and a JSON-payload inspector. Tool / extension filter chips and date-range UX deferred (the API supports `after`/`before` so it's a UX iteration). The ☰ menu doesn't exist yet — for v1 the entry sits next to "Inkorgen" in `MainNavigation`.

### Runtime track (closing the loop)

5. ~~**Stub Stina echo loop**~~ — landed; `@stina/orchestrator` ships the synchronous decision-turn entry point with a swappable `DecisionTurnProducer`. The v1 stub appends a canned acknowledgement after every user-authored post to a thread. SSE streaming is intentionally NOT wired yet — sub-millisecond stub latency doesn't justify it; that part lands with item 6 when real model latency shows up.
6. ~~**Real Stina with provider**~~ — landed in Phases 5b–6b. `DecisionTurnContext.memory` carries active standing instructions + linked-entity profile facts (5b); `createProviderProducer` assembles a system prompt + role-based `ChatMessage[]` and drains a `ChatStreamDispatcher` (5c); apps/api wiring resolves the user's default model config and dispatches via `ExtensionHost.chat()`, with safe fallback to the canned stub when no model is configured (5d); SSE streaming is wired server-side via `POST /threads/stream` and `POST /threads/:id/messages/stream` endpoints (6a); UI now consumes the stream end-to-end through both web (fetch + SSE) and Electron (IPC `threads-stream-event`) transports, with a placeholder Stina card that renders the live reply (6b). **Loose end remaining**: tool calls (`tool_start` / `tool_end`) are still ignored — routing them through the §06 severity gate is item 7.
7. ~~**Tool call routing with severity gate**~~ — landed (Phase 8). `createProviderProducer` gates every tool call by severity: low/medium execute freely; high requires an `AutoPolicy` (looked up via injected callback, fail-safe blocked when absent); critical and hallucinated names block unconditionally. Blocked calls: `tool_blocked` stream event, synthesized `role:'tool'` error back to model, `action_blocked` activity log entry with `chosen_alternative:'skip'`. Policy-authorized high tools: `auto_action` log entry with redacted I/O. ~~Policy creation UI~~ — landed (§06): `PoliciesView` + `PolicyRow` + `CreateForm` components; "Autonomi" nav entry; full HTTP + IPC + api-client wiring; 11 route tests. ~~Per-tool redactor wiring~~ — landed: `ToolRedactor` type wired end-to-end; host-side tools can declare a redactor function; the producer applies it before writing the `auto_action` log entry (`flagged_for_review: false`); tools without a declared redactor fall back to `[redacted: no redactor declared]` + `flagged_for_review: true`. **v1 gap**: ALL extension-registered tools (mail/calendar/work) currently fall into the no-redactor branch because redactor functions cannot cross IPC — closing the extension-IPC gap is a separate tracked step. **Remaining gaps**: Escalate and Solve-differently collision options (v2); scope evaluation in policy lookup (trigger_kinds, standing_instruction match — v2); extension-IPC redactor transport; ~~severity-change cascades on extension update~~ (landed in Phase 8k — uninstall cascade still deferred); progressive trust.
8. **Event-triggered threads from extensions** [L] — Phase 8a landed: `emitEvent` typed API on `ctx.events`, host trust-boundary validation (deny-list of `'user'`/`'stina'`, content/trigger kind pairing, `extension_id` stamping), thread spawn + decision turn end-to-end, dev-test extension exposes a `dev_test_emit_test_mail` driver tool. Phase 8b landed: failure mode framing (`applyFailureFraming` helper in `@stina/orchestrator`, `RUNTIME_EXTENSION_ID` constant in `@stina/core`, both apps wired). Phase 8c landed: pending-first-turn invisibility gate (`first_turn_completed_at` column, `markFirstTurnCompleted` repo method, `list()` default excludes pending, `runDecisionTurn` and `applyFailureFraming` lift the gate). Phase 8d landed: linked-entity derivation from typed AppContent (`deriveLinkedEntities` pure helper in `@stina/extension-host`; mail → `person` ref keyed by lowercased email; calendar → `calendar_event` ref; scheduled → `[]`; both apps wire it into `repo.create`). Phase 8f landed: ~~`emitEventInternal`~~ runtime-only API (`spawnTriggeredThread` shared helper in `@stina/orchestrator`; both apps expose `emitEventInternal` accepting full unions; untrusted-data wrapper fix for `RUNTIME_EXTENSION_ID + system`). Phase 8h landed: ~~degraded mode~~ (`DegradedModeTracker` in `@stina/orchestrator`; anchor = 5th failure; `extension_status: degraded_mode_entered/exited` AppMessages on anchor thread; `suppressNotification` returned from `applyFailureFraming`; one tracker per host in both apps). Phase 8j landed: ~~notifications~~ (`NotificationDispatcher` pub/sub; `markNotified` monotonic guard; `notifyOnSurface` flag; SSE + REST routes; Electron IPC bridge; `useNotificationStream` + bell button + dropdown in `@stina/ui-vue`; wired into `MainNavigation`). **Remaining**: ~~failure mode~~ (landed in `188f162`), ~~degraded mode~~ (landed in Phase 8h), ~~pending-first-turn UI invisibility gate~~ (landed in Phase 8c), ~~notifications~~ (landed in Phase 8j), ~~`emitEventInternal`~~ (landed in Phase 8f), ~~linked-entity derivation from AppContent~~ (landed in Phase 8d), v2 resolve-via-installed-extensions pass, ~~closing-action normalization~~ (landed in Phase 8g — v1 slice; deferred: multi-action contradiction, multiple-normal/silent retention), ~~scheduled-jobs migration to typed events~~ (landed in Phase 8i), and per-extension migrations of mail/calendar/work to use the new API.

### Recall track (§02 / §03)

R1. ~~**Recall provider extension API surface**~~ — landed (Phase 8e step A, `3223b33` + follow-ups). Permission, ctx surface, IPC message-pair, host handler, registry wiring in both apps, dev-test driver, end-to-end integration test via real worker. **Production consumer is still zero in this step** — see R2.
R2. ~~**`MemoryContextLoader` consumes `RecallProviderRegistry`**~~ — landed (Phase 8e step B). Per-entity recall queries run in parallel with profile-fact lookups; results render under `## Information från extensions` in the system prompt.
R3. ~~**`stina-ext-people` registers a recall provider**~~ — landed in sibling repo `stina-ext-people` (commit `5f4440c`, manifest declares `recall.register`, `src/recall/peopleRecallProvider.ts` matches lowercased email first (score 1.0), then name (score 0.8), skips archived people, honors `query.limit`, fire-and-forgets a `touchLastMentioned` bump on match. Renders persons as `"<name> — <relationship>. <description>"` capped at 300 chars. 7 unit tests). The full Recall track is now visible end-to-end: open a typical-morning thread from a known sender → People-registry blurb appears under `## Information från extensions` in Stina's system prompt.
R4. **`recall(query)` as a Stina-callable tool** [M] — built-in tool in the chat tool registry that calls `host.recallProviderRegistry.query` and `MemoryRepositories` in parallel and returns merged results. Currently `recall` only fires implicitly at thread-start via R2; this lets Stina pull in fresh context mid-turn. Out of scope for the recall-track v1 if R2 + R3 give acceptable UX.

### Cross-platform / migration track

9. ~~**Electron IPC bridge for threads**~~ — landed in `b7315d9`; both web and electron consume the same threads / messages / activity routes via their respective transports.
10. ~~**§08 migration + backup wiring**~~ — fully landed. `@stina/migration` ships `ChatMigrator`, `MigrationRunner`, `sanityChecks`, `schemaVersions`, `BackupWriter` (VACUUM INTO snapshot), `MigrationInterruptedError`, `runMigrationIfNeeded`, and `readMigrationMarker`. All three apps wire `runMigrationIfNeeded` after the early marker check. ~~Crash-recovery dialog~~ — landed: Electron shows `showMessageBoxSync` three-option dialog (Resume / Show Backup / Quit); API and TUI log a structured recovery message and exit cleanly before any subsystems initialize. **Remaining v1 gaps**: full gzip archive; quiescence / blocking new input; post-upgrade welcome thread; extension audit; settings translation; dry-run mode.
11. ~~**Playwright suite (Phase B)**~~ — landed. `POST /auth/e2e-session` (gated by `STINA_E2E`), `playwright.config.ts`, `e2e/global-setup.ts`, `e2e/fixtures.ts`, `e2e/inbox.spec.ts` (3 tests). Run via `cd apps/web && STINA_E2E=true pnpm e2e`.

### Documentation track

12. ~~**`docs/architecture.md` rewrite**~~ — landed in `57c6b82`; rewritten around the new package layer, three-actor model, and lifecycle diagram.
13. ~~**Developer guides**~~ [M] — ~~`adding-a-recall-provider.md`~~ (landed). ~~`adding-a-tool-with-severity.md`~~ (landed). ~~`adding-an-event-source-extension.md`~~ (landed). ~~`writing-a-migration.md`~~ (landed). All four §09 developer guides are complete.
14. **`stina.app` user docs** [L] — the public-facing user docs subsite per §09. Owned by maintainers; not a code task.

---

## Specific gotchas worth remembering

- **The `app` author trust boundary is structural.** `AppMessage.content` is `AppContent` (typed kind), not free text. Don't introduce a `string`-content escape hatch — that re-opens the prompt-injection vector §02 closed.
- **`mark*` helpers are monotonic.** `markSurfaced` and `markNotified` both no-op on second call. The intent is "set if not set"; do not refactor to "always update".
- **Dream-pass-origin mutations must use the guarded methods.** `mutateAsDreamPass` and `deleteAsDreamPass` exist in the memory repos for a reason — they enforce the §07 hard rule. Calling raw `update()` from a dream-pass code path bypasses the guard silently.
- **The `auto_action` activity-log kind requires policy + tool traceability.** Don't reuse it for "Stina did a thing" generally; that breaks recap rendering and audit semantics. If you need a different shape, add a new kind to the union in `@stina/core/autonomy/types.ts`.
- **Migrations use the existing `_migrations` table.** Each migration is named `<package>/<file>` (the runner derives the package name from the path). The `<package>` portion is part of the dedup key — never rename a package without thinking about whether old migrations need a compatibility shim.

---

## Where to look for things

| You want | Look in |
|----------|---------|
| Schema definition | `packages/<pkg>/src/db/schema.ts` |
| SQL migrations | `packages/<pkg>/src/db/migrations/*.sql` |
| Repository / runtime logic | `packages/<pkg>/src/db/repositories/` |
| Decision-turn orchestration | `packages/orchestrator/src/runDecisionTurn.ts` |
| Decision-turn producers (swappable) | `packages/orchestrator/src/producers/` |
| Thread-start memory loading (§03) | `packages/orchestrator/src/memory/MemoryContextLoader.ts` |
| Provider wiring (per-user model config) | `apps/api/src/redesignProvider.ts` |
| Shared types | `packages/core/src/<domain>/types.ts` |
| API routes | `apps/api/src/routes/threads.ts` (and friends) |
| API client | `packages/api-client/src/{types,client}.ts` |
| Vue components | `packages/ui-vue/src/components/views/InboxView.*` |
| State management for inbox | `packages/ui-vue/src/composables/useThreads.ts` |
| Test fixtures / scenarios | `packages/test-fixtures/src/scenarios/` |
| Seed CLI | `packages/test-fixtures/scripts/seed-dev-db.ts` |
| Specs (canonical) | `docs/redesign-2026/0X-*.md` |

---

## How to verify the world is healthy

```sh
pnpm typecheck && pnpm test
```

Should print 620 tests passed, no typecheck errors. If either fails, that's the regression to fix before doing anything else.

A quick smoke test that the app actually boots and migrations land:

```sh
DB_PATH=/tmp/stina-smoke.db pnpm dev:seed typical-morning --fresh
sqlite3 /tmp/stina-smoke.db "SELECT COUNT(*) FROM threads"   # → 7
sqlite3 /tmp/stina-smoke.db ".tables"                         # all redesign-2026 tables present
```

If migrations fail to apply (no new tables), check that the new package's `tsup.config.ts` has `splitting: false`. That's the most common breakage.
